source :rubygems

gem 'rack'    # Just let it work with latest. If the API breaks, I'll fix it
gem 'nokogiri'
gem 'jsmin'
gem 'dalli'

group :test do
  gem 'rspec',    '2.3.0'
  gem 'steak',    '1.0.0'
  gem 'capybara', '0.4.0'
  gem 'redis'
  gem 'memcached'
end
